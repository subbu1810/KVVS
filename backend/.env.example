# Port Configuration
PORT=5000

# Security Credentials
JWT_SECRET=quantum_zero_secret_token_key_3026

# MySQL Database Configurations (Leave blank to automatically fallback to local SQLite database)
DB_HOST=
DB_PORT=3306
DB_NAME=quantum_power_db
DB_USER=root
DB_PASS=

# Razorpay Gate Credentials (Set to MOCK or leave blank to enable Simulated Checkout sandbox mode)
RAZORPAY_KEY_ID=MOCK
RAZORPAY_KEY_SECRET=mock_secret_key_12345

# SMTP Nodemailer Configurations (Leave blank to write emails directly to terminal logs)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME="Quantum Power Industries"
